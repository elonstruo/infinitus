#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_z-schema@4.2.3@z-schema/bin/z-schema" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_z-schema@4.2.3@z-schema/bin/z-schema" "$@"
  ret=$?
fi
exit $ret
